# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test / tooling artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor/OS cruft
.DS_Store
.idea/
.vscode/

# Note: uv.lock is intentionally NOT ignored — this is an application
# (CLI tool), not a library, so the lockfile is committed for reproducible
# installs/CI.

